FROM node:16-alpine

WORKDIR /usr/src/app

# Copy package manifests
# Using yarn as per the project's lock file
COPY package.json yarn.lock ./

# Install dependencies
RUN yarn install

# Copy the rest of the source code
COPY . .

# Expose port and start the development server
EXPOSE 3000
CMD ["yarn", "start:dev"]
