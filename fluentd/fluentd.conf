<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<match *.**>
  @type copy
  <store>
    @type stdout
  </store>
  <store>
    @type file
    path /fluentd/log/app.log
    append true
    <buffer>
      timekey 1d
      timekey_use_utc true
    </buffer>
  </store>
</match>
